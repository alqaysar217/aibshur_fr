rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow ALL reads and writes for all collections
    // This is to prevent UID mismatch issues in Firebase Studio during development.
    match /{document=**} {
      allow read, write: if true; 
    }
  }
}