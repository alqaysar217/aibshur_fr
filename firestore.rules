
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- دالات مساعدة ---
    function isSignedIn() {
      return request.auth != null;
    }

    // للنموذج الأولي، نعتبر كل مستخدم مسجل كمسؤول مؤقتاً لضمان عمل لوحة التحكم
    function isAdmin() {
      return isSignedIn();
    }

    // --- المجموعات العامة ---
    match /cities/{cityId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /categories/{catId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /stores/{storeId} {
      allow read: if true;
      allow write: if isAdmin();

      match /products/{productId} {
        allow read: if true;
        allow write: if isAdmin();
        
        match /variants/{vId} {
          allow read: if true;
          allow write: if isAdmin();
        }
      }
    }

    // --- المنتجات (البحث الموحد عبر المتاجر) ---
    match /{path=**}/products/{productId} {
      allow read: if true;
    }

    // --- ملفات المستخدمين ---
    // السماح بالقائمة (List) لمجموعة المستخدمين - أساسي للوحة الإدارة
    match /users {
      allow list: if isAdmin();
    }

    match /users/{userId} {
      // السماح بالقراءة والكتابة للمستخدمين المسجلين
      allow read, write: if isSignedIn();
      // السماح بعمليات القائمة (Queries) للمسؤول
      allow list: if isAdmin();
      
      // الوصول لكافة المجموعات الفرعية (المحفظة، العناوين، التنبيهات، إلخ)
      match /{allSubcollections=**} {
        allow read, write: if isSignedIn();
      }
    }

    // --- الطلبات ---
    // السماح بالقائمة (List) لمجموعة الطلبات - أساسي للوحة الإدارة
    match /orders {
      allow list: if isAdmin();
    }

    match /orders/{orderId} {
      allow read, write: if isSignedIn();
      allow list: if isSignedIn();
    }
    
    // دعم الاستعلامات المجمعة للطلبات (Collection Group)
    match /{path=**}/orders/{orderId} {
      allow read, write: if isSignedIn();
      allow list: if isSignedIn();
    }

    // --- إعدادات النظام ---
    match /admin_settings/{settingId} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    // --- الإعلانات ---
    match /ads/{adId} {
      allow read: if true;
      allow write: if isAdmin();
    }
  }
}
